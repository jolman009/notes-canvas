# =============================================================================
# Canvas Notes App — Production Environment Variables
# =============================================================================
# Copy this file to .env and fill in values for your environment.
# NEVER commit .env to version control.
#
# In Vercel: set these in Project Settings → Environment Variables.
# =============================================================================

# --- Required (Client + Server) ---
# Supabase project URL (e.g., https://abc123.supabase.co)
SUPABASE_URL=

# Supabase anonymous/public API key
# Exposed to client via envPrefix in vite.config.ts
SUPABASE_ANON_KEY=

# --- Required (Server Only) ---
# Supabase service role key for admin operations (RLS bypass)
# MUST NOT be exposed to client — verified by scripts/verify-service-role-exposure.mjs
SUPABASE_SERVICE_ROLE_KEY=

# --- Optional (Client Only) ---
# Sentry DSN for error tracking. If absent, Sentry is silently disabled.
VITE_SENTRY_DSN=
