# Supabase (preferred production backend)
SUPABASE_PROJECT_ID=
SUPABASE_ANON_KEY=
# Optional, auto-derived from project ID when empty
# SUPABASE_URL=
# Optional table/row override
# SUPABASE_TABLE=app_state
# SUPABASE_ROW_ID=main

# Direct Postgres backend (fallback if Supabase vars are not set)
# DATABASE_URL=

# Local SQLite fallback (used when neither Supabase nor DATABASE_URL are set)
# SQLITE_PATH=.data/notes.db
